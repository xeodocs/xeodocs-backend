apiVersion: apps/v1
kind: Deployment
metadata:
  name: project
  labels:
    app: project
spec:
  replicas: 1
  selector:
    matchLabels:
      app: project
  template:
    metadata:
      labels:
        app: project
    spec:
      containers:
      - name: project
        image: registry:12030/xeodocs-backend-project:latest
        imagePullPolicy: Never  # Optional: Skips external pulls for local images
        env:
        - name: DATABASE_URL
          value: "postgres://user:password@db:5432/xeodocs_db?sslmode=disable"
        - name: PROJECT_PORT
          value: "80"
        - name: JWT_SECRET
          value: "your-secret-key"
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: project
  labels:
    app: project
spec:
  selector:
    app: project
  ports:
  - port: 80
    targetPort: 80
